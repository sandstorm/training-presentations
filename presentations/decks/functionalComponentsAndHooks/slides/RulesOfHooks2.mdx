import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

<CodeSurfer>

```tsx showNumbers title="Rules of Hooks" subtitle="Don't Do this! )"
const MyComponent = (props) => {
    // ... some code

    if (props.loading) {
        useEffect(() => {
            // do something
        })
    }

    return // some code
}
```

```diff 5:8
```

</CodeSurfer>
