import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

<CodeSurfer>

```tsx showNumbers title="useEffect() - with dependencies"
const MyComponent = ({ someProp }) => {
    // ... some code

    useEffect(() => {
      // The code in here will only be run if 'someProp' has changed
    }, [someProp])

    return // .... some code
}
```

```diff 1:9
```

```diff 6
```

</CodeSurfer>
