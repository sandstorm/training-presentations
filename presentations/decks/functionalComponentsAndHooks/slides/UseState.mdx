import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"


<CodeSurfer>

```tsx showNumbers title="useState()" subtitle="Used to keep and change state of a component over multiple render cycles"
const [state, setState] = useState(initialState)
```

</CodeSurfer>
