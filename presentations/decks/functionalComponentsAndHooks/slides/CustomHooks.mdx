## Custom Hooks

* You can extract logic into reusable custom hooks
* Hook names should always start with **use** (important convention)
* Important: two components using the same hook **do not share the same state**
