import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

<CodeSurfer>

```tsx showNumbers title="Class Components" subtitle="Doing react the \"old school way\""
import React from 'react'

type ArticleProps = {
    title: string
    text: string
}

class Article extends React.PureComponent<ArticleProps> {
  render() {
    return (
        <article className="article">
            <h2 className="title">{this.props.title}</h2>
            <p className="text">{this.props.text}</p>
        </article>
    )
  }
}

export default Article
```

```diff 8:17
```

</CodeSurfer>

<Notes>

Make clear that this has basically nothing to do with traditional OOP and
is merely an implementation detail of the old class based API.

</Notes>
