import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

## Arrays and Objects

<ul>

<Appear>

<li> Do **not** use `push`, `pop` etc. as these operations are all mutable </li>
<li> Create new Array/Object instances with Array/Object-spread </li>
<li> Use libraries for working with data structures (e.g. **Immer**, **ImmutableJS**) </li>
<li> Caution: some methods like `Array.sort()` operate mutably 😉 </li>

</Appear>

</ul>
