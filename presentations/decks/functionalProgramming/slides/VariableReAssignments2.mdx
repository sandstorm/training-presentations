import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

import "prismjs/components/prism-tsx"

<CodeSurfer>

```tsx showNumbers title="Variable Reassignment"
// Mutable way
const getRoleLogoPath = (role) => {
    let logo

    if (role === 'admin') {
      logo = '../somePath/adminLogo.png'
    } else if (role === 'mod') {
      logo = '../somePath/modLogo.png'
    } else {
      logo = '../somePath/userLogo.png'
    }

    return logo
}

// Immutable way
const getRoleLogoPathImmutably = (role) => {
    if (role === 'admin') {
      return '../somePath/adminLogo.png'
    }

    if (role === 'mod') {
      return '../somePath/modLogo.png'
    }

    return '../somePath/userLogo.png'
}
```

```diff 3
```

```diff 6
```

```diff 13
```

```diff 16:27
```

```diff 18:20
```

```diff 26
```

</CodeSurfer>
