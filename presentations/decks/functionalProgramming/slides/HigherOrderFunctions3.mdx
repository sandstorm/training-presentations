import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

import "prismjs/components/prism-tsx"

<CodeSurfer>

```tsx title="Higher-order Functions"
const add = (a) => (b) => a + b

const calculate = (operatorFn, operandA, operandB) => {
    return operatorFn(operandA, operandB)
}
```

```diff 1
```

```diff 3:5
```

</CodeSurfer>
