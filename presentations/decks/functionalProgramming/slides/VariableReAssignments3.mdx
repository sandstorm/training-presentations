import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

import "prismjs/components/prism-tsx"

<CodeSurfer>

```tsx showNumbers tile="Variable Reassignment"
// Mutable way
const sumList = (numberList) => {
    let sum = 0

    for (let index = 0 index < numberList.length index++) {
      sum = sum + numberList[index]
    }

    return sum
}

// Immutable way
const sumListImmutably = (numberList) => {
    return numberList.reduce((sum, number) => {
      return sum + number
    }, 0)
}
```

```diff 1:10
```

```diff 3:7
```

```diff 12:17
```

```diff 14:16
```

</CodeSurfer>
