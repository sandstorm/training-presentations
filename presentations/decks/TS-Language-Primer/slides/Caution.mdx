import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

<CodeSurfer>

```ts title="Caution!" subtitle="Always use the lower case type for primitives"
const str: string = new String() // type string !== type String
/**
Type 'String' is not assignable to type 'string'.
  'string' is a primitive, but 'String' is a wrapper object. Prefer using 'string' when possible.
*/
```

```diff 3,4 title="Caution!" subtitle="TypeScript Error Message"
```

</CodeSurfer>
