import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

<CodeSurfer>

```ts title="Type Alias" subtitle="Give type a name to reference it later"
type Vector2d = [number, number]

const myVector: Vector2d = [2, 2]

const addVectors = (a: Vector2d, b: Vector2d): Vector2d => {
  // implementation detail
}
```

</CodeSurfer>
