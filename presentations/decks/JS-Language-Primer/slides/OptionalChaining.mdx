import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer"

<CodeSurfer>

```tsx showNumbers title="Optional chaining with elvis operator (.?*)" subtitle="Old way of savely traversing objects"
const user : {}
let street = undefined

if (user && user.address && user.address.street) {
    street = user.address.street
}

console.log(street)
```

```diff 4[5:47] title="Optional chaining with elvis operator (.?*)" subtitle="Old way of savely traversing objects"
```

```diff 8 title="Optional chaining with elvis operator (.?*)" subtitle="Old way of savely traversing objects"
let street = undefined

if (user && user.address && user.address.street) {
    street = user.address.street
}

console.log(street)
```

```tsx showNumbers title="Optional chaining with elvis operator (.?*)" subtitle="Old way of savely traversing objects"
let street = undefined

if (user && user.address && user.address.street) {
    street = user.address.street
}

console.log(street) // => undefined
```

```tsx showNumbers title="Optional chaining with elvis operator (.?*)" subtitle="With optional chaining"
let street = undefined

if (user && user.address && user.address.street) {
    street = user.address.street
}

console.log(street) // => undefined

const elvisStreet = user?.address?.street

console.log(elvisStreet)
```

```tsx showNumbers title="Optional chaining with elvis operator (.?*)" subtitle="With optional chaining"
let street = undefined

if (user && user.address && user.address.street) {
    street = user.address.street
}

console.log(street) // => undefined

const elvisStreet = user?.address?.street

console.log(elvisStreet) // => undefined
```

</CodeSurfer>
